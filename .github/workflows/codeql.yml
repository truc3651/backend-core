name: CodeQL

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
  schedule:
    - cron: '33 6 * * 0'

jobs:
  analyze:
    uses: truc3651/github-workflows/.github/workflows/codeql.yml@master
